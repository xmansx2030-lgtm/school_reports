# SECURITY SECRET ROTATION PLAN

تاريخ الفحص: 2026-09-12. هذا المستند لا يحتوي أي قيمة سرية أو بصمة يمكن
استعمالها. لم تُقرأ ملفات البيئة المحلية غير المتتبعة، ولم يُنفذ تدوير أو
إعادة كتابة تاريخ أو اتصال بمزود إنتاج.

## الحالة الحالية

- فحص `scripts/check_no_tracked_secrets.py` نجح: لا يوجد في `HEAD` ملف أسرار
  محظور متتبع (`.env`، مفاتيح خاصة، قواعد SQLite أو ملفات بيئة الخادم).
- يوجد ملف `.env` محلي ignored؛ لم يُفحص أو يُعدّل لأنه خارج Git وقد يحتوي
  إعدادات المشغّل.
- لا تتوفر أداة entropy scanner خارجية (`gitleaks`/`trufflehog`) في البيئة؛
  الفحص الحالي يجمع حارس الملفات المتتبعة، Bandit، والمراجعة التاريخية
  الموجّهة أدناه. يلزم تشغيل scanner مخصص في CI لإغلاق هذا النقص نهائيًا.

## التعرض التاريخي المثبت

ظهر `.env` في 19 commit ضمن كل المراجع المحلية: 17 لقطة ذات محتوى، ثم حذفه
نهائيًا في `8c9fed3a42b6f1a2b9d5bf229ae1bf3e5b5c3225` بتاريخ
2026-03-06T04:33:35+03:00. أول ظهور مرصود كان في
`32b7e8587f5e9fcb6c7e078e3a5aa8fce9130135` بتاريخ
2025-09-04T21:39:02+03:00.

| نوع القيمة | المزود/المالك | مدى الظهور التاريخي | نسخ مختلفة | الصلاحية/التدوير |
|---|---|---|---:|---|
| Django `SECRET_KEY` | التطبيق | 2025-09-04 إلى 2026-03-06 | 4 | غير مثبت |
| `DATABASE_URL` | قاعدة البيانات/مزود الاستضافة السابق | 2025-09-04 إلى 2025-12-16 | 3 | غير مثبت |
| Cloudinary API secret | Cloudinary | 2025-09-04 إلى 2025-12-17 | 3 | غير مثبت |
| Cloudinary API key/name | Cloudinary | 2025-09-04 إلى 2025-12-17 | 3 | غير مثبت |

الأعداد حُسبت بمقارنة SHA-256 داخل الذاكرة وعُرض عدد القيم فقط؛ لم تُطبع
القيم ولا البصمات.

## EXTERNAL ACTION REQUIRED

1. مسؤول Django يثبت إن كان كل `SECRET_KEY` تاريخي قد أُلغي. إن لم يثبت،
   يولّد مفتاحًا جديدًا، يخطط لأثر الجلسات/التواقيع، ويحدّث secret store.
2. مسؤول قاعدة البيانات يدوّر كل user/password ظهر ضمن `DATABASE_URL`، يراجع
   صلاحياته وسجلات الوصول، ويعطل الحسابات القديمة.
3. مسؤول Cloudinary يدوّر API secret والمفاتيح القابلة للتدوير، يقيّد
   الصلاحيات، ويراجع usage logs خلال فترة التعرض.
4. يسجل المسؤول لكل بند: وقت التدوير، معرف التذكرة/السجل، ومن تحقق من إبطال
   القيمة القديمة. لا توضع القيمة الجديدة أو القديمة في هذا المستند.
5. بعد الإبطال فقط، يُشغّل secret scanner كامل على كل refs وفي منصة Git
   البعيدة، لأن refs المحلية لا تثبت ما في forks أو caches.

## قرار إعادة كتابة التاريخ

إعادة كتابة التاريخ ليست بديلًا عن التدوير. قد تكون مفيدة بعد الإبطال لتقليل
التسريب العرضي، لكنها تغيّر كل commit SHA وتتطلب نافذة منسقة، نسخًا احتياطية،
إبطال clones القديمة، وforce push. لذلك لم يُستخدم `git filter-repo` أو force
push في هذه المرحلة. القرار بشري وصريح بعد إغلاق إجراءات التدوير أعلاه.

