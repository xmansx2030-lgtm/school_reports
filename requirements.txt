asgiref==3.9.1
Brotli==1.2.0
cffi==2.0.0
cbor2==6.1.4
cryptography==50.0.0
cssselect2==0.8.0
fonttools==4.60.2
Pillow==12.3.0
psycopg2-binary==2.9.10
pycparser==2.22
pydyf==0.11.0
pyphen==0.17.2
python-dotenv==1.2.2
sqlparse==0.6.0
tinycss2==1.5.1
tinyhtml5==2.0.0
tzdata==2025.2
Unidecode==1.3.8
webencodings==0.5.1
zopfli==0.2.3.post1

daphne==4.2.3

# Django and related
Django==5.2.17
channels==4.1.0
channels-redis==4.2.0
uvicorn==0.30.6
websockets==14.2
gunicorn==23.0.0
whitenoise==6.11.0
dj-database-url==3.0.1

# Rate limiting
django-ratelimit==4.1.0

# REST API
djangorestframework==3.17.2

# Cloudflare R2 (S3-compatible)
django-storages==1.14.6
boto3==1.43.56

# Redis & Caching
django-redis==5.4.0
redis==5.0.1

# Excel handling
openpyxl==3.1.5

# PDF
weasyprint==69.0
reportlab>=4.2,<5
arabic-reshaper>=3,<4
python-bidi>=0.6,<1

# Hijri date conversion (pure-Python, no native deps)
hijri-converter==2.3.1

# MIME detection (optional; validators fall back if unavailable)
python-magic==0.4.27

# Celery
celery==5.4.0
django-celery-results==2.5.1

# Standards-based Web Push for installed PWAs (VAPID + payload encryption)
pywebpush==2.4.0

# Native Android/tablet operations alerts (FCM HTTP v1 OAuth)
google-auth==2.56.3
requests==2.34.2

# Markdown rendering (User guide page)
Markdown==3.10.3

# Optional production error monitoring. It remains disabled until SENTRY_DSN is set.
sentry-sdk[django]==2.66.1
