# فحص رحلة المعلم ذي الاشتراك الشخصي — 2026-09-25

## النطاق والبيئة

- فُحص الكود والشاشات محليًا على قاعدة SQLite منفصلة في مجلد Temp، مع حساب وباقة وشواهد اصطناعية.
- فُحص الدفع فعليًا عبر **بيئة ميسّر الاختبار** ومفتاح `sk_test_` الخاص بالمشروع؛ لم تُحصّل أموال حقيقية. لا يتضمن هذا التقرير المفتاح أو بيانات جلسة المتصفح.
- فُحص الموقع العام `https://tawtheeq-ksa.com/` للقراءة فقط. النسخة المنشورة هناك لا تعرض عرض المعلم الشخصي، ومسار `/personal/register/` يعيد 404. لم تُنشر تغييرات هذا الفحص.

## نتائج الرحلة

| المحطة | دليل الفحص | النتيجة |
| --- | --- | --- |
| صفحة الهبوط والباقات | عرض الباقة المجانية والمدفوعة على الحاسوب والجوال؛ رابط الباقة المدفوعة يقود للتسجيل مع الاحتفاظ بالاختيار | نجح محليًا بعد إصلاح معرّف الباقة والتوجيه |
| التسجيل | اختبار HTTP لإنشاء حساب ومعه مساحة واشتراك مجاني، دون مدرسة أو عضوية مدرسية؛ اختبار الحساب الحالي | نجح |
| الدخول لحساب قائم | تسجيل دخول بحساب اصطناعي من رابط الخطة المدفوعة؛ الوصول إلى تأكيد الباقة المختارة | نجح |
| تأكيد الاشتراك | ظهور السعر 49 ريال، المدة 90 يومًا، السعات وبريد الفاتورة قبل بوابة الدفع | نجح |
| الدفع الإلكتروني | إنشاء فاتورة اختبار لدى ميسّر، إدخال بطاقة الاختبار العامة، اجتياز محاكي 3DS، العودة إلى المنصة | نجح؛ فاتورة ميسّر `927eb0ba-4939-47b9-b2a7-c302b3c99d6d` |
| التفعيل والفاتورة | تحوّلت الدفعة المحلية `de292cba-cbf5-48da-9fd8-fd185ceeee11` إلى `paid`، والاشتراك إلى نشط حتى 2026-12-23؛ تنزيل فاتورة PDF من صفحة الاشتراك | نجح محليًا؛ الفاتورة صفحة A4 واحدة واضحة |
| الاستفادة من المساحة | إنشاء تقرير مكتمل، إضافة شاهد برابط، رفع صورة شاهد اصطناعية وتنزيلها، ظهورها في ملف الإنجاز وصفحة الطباعة | نجح |
| العزل والصلاحيات | اختبارات تمنع حسابًا آخر من الوصول إلى التقرير والشاهد والفاتورة أو ربط شاهد بتقرير الغير | نجح في الاختبارات |
| الفشل والتجديد | اختبار فشل فاتورة دون تفعيل أو فاتورة قابلة للتنزيل؛ اختبار تجديد متكرر دون فقد حالة الاشتراك أو الأيام المتبقية | نجح في الاختبارات |

## تقييم تجربة الاستخدام

تقييم فحص خبير محلي: **4/5**. السعر والمدة وما يحدث بعد الدفع واضح قبل الانتقال إلى ميسّر. اكتملت رحلة إنشاء التقرير والشاهد والملف السنوي من واجهة الجوال دون تمرير أفقي عند عرض 390 بكسل. نموذج التسجيل طويل (ثمانية مدخلات منها اسم المدير الاختياري)، لكنه يوضح أن اسم المدرسة والمدير للتعريف فقط. هذا تقييم إرشادي، وليس دراسة مستخدمين أو قياس تحويلات حقيقية.

أُصلحت ملاحظات مرئية: تباين زر طباعة ملف الإنجاز وزر الاشتراك داخل لوحة المعلم، تسمية حقل التقرير المرتبط بالعربية، وترتيب السنة في صفحة الطباعة. تعرض صفحة ميسّر المستضافة **شعارًا افتراضيًا** في حساب الاختبار؛ شعار الفاتورة التي تنتجها المنصة سليم. شعار صفحة ميسّر يُضبط من لوحة ميسّر، وقد يؤثر ضبطه على فواتير أخرى في الحساب نفسه.

## تحقق آلي وحدود النتيجة

- `manage.py test personal reports.tests.test_landing_page --settings=config.test_settings --noinput`: **34 اختبارًا ناجحًا**.
- `ruff check personal`, و`manage.py check`, و`makemigrations --check --dry-run`: ناجحة، دون هجرة جديدة.
- لا يُثبت اختبار بيئة ميسّر التجريبية نجاح المفتاح الحي أو التحصيل والإيداع في الإنتاج. كما لم يُتحقق من وصول رسالة الفاتورة إلى صندوق بريد خارجي؛ تحقق الفحص المحلي من تسجيل إرسالها وتنزيل ملفها.
- يلزم نشر هذه النسخة أولًا قبل إعادة اختبار رابط التسجيل والدفع على النطاق العام، ثم التحقق من إعدادات ميسّر والبريد الحية باستخدام عملية دفع معتمدة ومحددة القيمة.
